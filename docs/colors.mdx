---
layout: "docs"
title: "Colors"
description: "A flexible color palette for your project."
prev: "variables.html"
next: "reset.html"
---

LowCSS comes equipped with a carefully curated default color palette designed to streamline your design process and enhance the visual appeal of your web projects. This palette serves as a foundation for consistent and harmonious color choices, allowing you to effortlessly create stunning user interfaces.

<Separator />

# Available colors

Here are the colors available in the default palette:

<div className="">
{Array.from(new Set(Object.keys(props.site.low.colors).filter(c => c.endsWith("00")).map(c => c.split("-")[0]))).map(key => (
    <div key={key} className="mb-4">
        <div className="text-lg font-bold mb-1">{key}</div>
        <div className="w-full grid grid-cols-3 md:grid-cols-9 gap-2">
        {Object.keys(props.site.low.colors).filter(c => c.startsWith(key)).map(color => (
            <div key={color} className="w-full">
                <div className={`bg-${color} rounded w-full h-10 mb-2`} />
                <div className="text-xs text-gray-700 font-bold">{color.split("-")[1]}</div>
                <div className="text-3xs text-gray-500">{props.site.low.colors[color]}</div>
            </div>
        ))}
        </div>
    </div>
))}
</div>

<Separator />

# Basic usage

For convenience and rapid development, you can apply color classes directly to your HTML elements using background, border and text utilities:

```html
<div class="bg-yellow-200 text-gray-800">
    This element has a yellow background color and dark text.
</div>
```

<Separator />

# Using the color palette with SASS

You can harness the power of the default color palette within your SASS styles by utilizing the predefined color variables. This approach allows you to maintain consistency across your styles while leveraging the flexibility of SASS.

You will need to import the `low/colors` module in your SASS file in order to use the default color palette:

```scss
@use "low/colors" as colors;
```

## colors.get-color($color-name)

Use the `get-color` function to get a color from the color palette:

```scss
@use "low/colors" as colors;

.btn {
    background-color: colors.get-color("blue-500");
}
```
