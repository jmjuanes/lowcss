---
layout: "docs"
title: "Utilities API"
description: "The reference for all LowCSS utilities."
prev: "utilities-map.html"
---

<Separator />

{props.utilities.map(utility => (
<div className="mb-16" key={utility.key}>
    <div className="text-xl font-bold mb-4">
        <a href={`#${utility.key}`} name={utility.key} className="text-gray-800 hover:text-gray-900">{utility.key}</a>
    </div>
    <div className="mb-4">
        {utility.attributes.description}
    </div>
    {(utility.responsive || utility.pseudos?.length > 0) > 0 && (
        <div className="mb-4 d-flex gap-2 flex-wrap">
            {utility.responsive && (
                <div className="py-1 px-2 bg-gray-800 text-white text-xs r-md">
                    <span className="font-bold">responsive</span>
                </div>
            )}
            {utility.pseudos.map(v => (
                <div key={v} className="py-1 px-2 bg-gray-300 text-xs r-md">
                    <span>pseudo: <span className="font-bold">{v}</span></span>
                </div>
            ))}
        </div>
    )}
    <div className="maxh-96 overflow-auto text-gray-300 scrollbar">
        <table className="w-full">
            {Object.entries(utility.values).map(item => (
            <tr key={item[0]} className="b-solid b-1 b-gray-300 text-xs">
                <td className="p-4 font-mono bg-gray-200 text-blue-600">
                    <span>{utility.classname}-{item[0]}</span>
                </td>
                <td className="p-4 font-mono text-gray-700">
                {utility.properties.map(p => (
                    <div key={p} className="mb-1">
                        <span className="text-indigo-700">{p}</span>: {item[1]};
                    </div>
                ))}    
                </td>
            </tr>
            ))}
        </table>
    </div>
</div>
))}
